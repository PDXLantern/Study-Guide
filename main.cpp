// Carlos Rivera
// main.cpp file

#include "main.h"

int main()
{
	List active_list;
	active_list.command(0);
	std::cout << std::endl;
	active_list.command(1);
	std::cout << std::endl;
	active_list.command(0);
	return 0;
}
